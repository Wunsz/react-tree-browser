import { TreeBrowser, withTreeBrowser } from './index';

describe('TreeBrowser', () => {
    it('is truthy', () => {
        expect(TreeBrowser).toBeTruthy();
    });
});

describe('withTreeBrowser', () => {
    it('is truthy', () => {
        expect(withTreeBrowser).toBeTruthy();
    });
});
